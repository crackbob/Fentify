{
	"name": "fentify",
	"version": "2.0.0",
	"description": "Fentify v2",
	"main": "src/index.js",
	"scripts": {
		"build": "esbuild src/index.js --minify --bundle --loader:.css=text --outfile=dist/fentify.js",
		"watch": "esbuild src/index.js --minify --bundle --loader:.css=text --outfile=dist/fentify.js --watch"
	},
	"dependencies": {
		"esbuild": "^0.25.11"
	}
}
